# 🚀 MikroTik ROS Local Update Server WIP

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![\.NET](https://img.shields.io/badge/.NET-9.0-purple)
![License](https://img.shields.io/badge/license-ELv2-orange)
![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20Linux-lightgrey)

**Локальный сервер обновлений для устройств MikroTik RouterOS**

_Кэшируйте прошивки, управляйте версиями и обновляйте устройства без доступа в Интернет_

</div>

## ✨ Особенности

- 🔧 Полная локальная эмуляция серверов обновлений MikroTik
- 📦 Управление версиями RouterOS v6 и v7 с поддержкой нескольких архитектур
- 🧩 Настройка допустимых архитектур (Allowed Arches) через веб-интерфейс
- 🕐 Автоматическая проверка обновлений по расписанию
- 📊 Интерактивная панель мониторинга:
  - средняя скорость загрузки прошивок
  - время последней проверки обновлений
  - дни без сбоев (uptime)
  - средний размер версий
- 🚨 Real-time алерты:
  - мало свободного места
  - проблемы с доступом в Интернет
  - общее состояние сервиса обновлений
- 📋 Подробные логи и встроенный просмотрщик
- 📈 Базовая аналитика логов (количество по уровням, активность по времени)
- 🌗 Переключаемые темы: тёмная и светлая с сохранением выбора
- 🔒 Безопасность: базовый hardening HTTP-заголовков
- ⚡ Высокая производительность: сжатие ответов и кэширование

## 🚀 Быстрый старт

### Требования

- .NET 9.0 Runtime или выше
- 500 МБ свободного места на диске
- Доступ в Интернет (для первоначальной загрузки прошивок)
- Современный браузер для работы с веб-интерфейсом

### Установка

1. **Скачайте последнюю версию** из раздела Releases вашего репозитория
2. **Распакуйте архив** в выбранную директорию
3. **Запустите приложение:**
   - Windows: `MikroTik.UpdateServer.exe`
   - Linux: `./MikroTik.UpdateServer`
4. **Откройте браузер** по адресу `http://localhost:5000`

## ⚙️ Настройка MikroTik

Добавьте DNS-запись на вашем MikroTik устройстве:

```routeros
/ip dns static add name="upgrade.mikrotik.com" address=<IP_ВАШЕГО_СЕРВЕРА>
```

Проверьте обновления:

```routeros
/system package update check-for-updates
/system package update install
```

## 🎯 Использование

### 📊 Панель управления (Dashboard)

Панель открывается по умолчанию и показывает:

- Quick Stats:
  - средняя скорость загрузки прошивок
  - время последней проверки обновлений
  - дни без сбоев
  - средний размер версий, хранящихся локально
- Статус сервера:
  - наличие доступа в Интернет
  - оценка свободного места
  - общее состояние сервиса обновлений
- Переключатель темы (иконка 🌙 / ☀️ в левом сайдбаре)

С панели также можно запускать ручную проверку обновлений.

### 📦 Управление версиями

Раздел версий позволяет:

- Просматривать доступные версии RouterOS v6 и v7
- Видеть размер каждой версии и дату загрузки
- Искать и сортировать версии по имени, дате и размеру
- Отмечать избранные версии (favorites) для быстрого доступа
- Задавать активную версию, которую будут получать устройства
- Удалять ненужные или устаревшие сборки

Отдельно настраиваются **допустимые архитектуры (Allowed Arches)**, чтобы контролировать, какие типы устройств могут получать обновления с этого сервера.

### 📅 Планировщик

Планировщик отвечает за автоматическую проверку новых версий RouterOS:

- Задание интервала проверки (в минутах)
- Привязка времени проверки к серверному времени
- Временная приостановка и возобновление автопроверок
- Отображение текущего статуса планировщика (активен / приостановлен)

### 📋 Логи и диагностика

Подсистема логов включает:

- Встроенный viewer логов в веб-интерфейсе
- Фильтрацию по уровню (Debug / Info / Warning / Error)
- Поиск по тексту сообщения
- Экспорт логов в ZIP-архив
- Базовую аналитику:
  - количество записей по уровням
  - активность по времени

## 🌗 Темы интерфейса

Интерфейс поддерживает тёмную и светлую тему.

- Переключение выполняется кнопкой в левом сайдбаре.
- Выбор темы сохраняется в Local Storage браузера.
- При первом запуске используется системная настройка (`prefers-color-scheme`), затем приоритет у явного выбора пользователя.

## 🔧 API Endpoints

Основные HTTP-эндпоинты сервера (примерная схема):

| Метод | Endpoint                            | Описание                                   |
|-------|-------------------------------------|--------------------------------------------|
| GET   | `/api/dashboard`                    | Сводная статистика для панели управления   |
| GET   | `/api/versions`                     | Список доступных версий                    |
| POST  | `/api/update-check`                 | Принудительная проверка обновлений         |
| POST  | `/api/set-active-version/{version}` | Установка активной версии                  |
| GET   | `/api/status`                       | Текущий статус сервера                     |
| GET   | `/api/logs`                         | Просмотр логов                             |
| GET   | `/api/logs/stats`                   | Аггрегированная статистика по логам        |
| GET   | `/api/allowed-arches`               | Получить список допустимых архитектур      |
| POST  | `/api/allowed-arches`               | Сохранить настройки допустимых архитектур  |

Актуальный список и параметры смотрите в коде контроллеров.

## 🛠️ Разработка

### Сборка из исходного кода

```bash
git clone https://github.com/Shepard199/mikrotik-update-server.git
cd mikrotik-update-server
dotnet restore
dotnet build
dotnet run
```

### Конфигурация

Сервер использует стандартную конфигурацию Kestrel. Базовый пример:

```json
{
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:5000"
      }
    }
  }
}
```

Вы можете переопределить порт и адрес через `appsettings.json` или переменные окружения.

## ⚠️ Отказ от ответственности

Этот проект не связан с компанией MikroTik.
Используйте на свой страх и риск.
Автор не несёт ответственности за возможные проблемы с устройствами.

## 📄 Лицензирование (Licensing)

Этот проект распространяется под двойным лицензированием, что позволяет пользователям выбирать между двумя различными наборами условий:

### 1. GNU General Public License (GPLv3)

Проект доступен под лицензией **GPL версии 3** для использования в проектах, совместимых с условиями GPL.
Вы можете использовать, изменять и распространять этот код при условии, что любые производные работы также будут опубликованы под той же лицензией (копилефт).

Полный текст лицензии находится в файле **`LICENSE.GPL-3.0`**.

### 2. Коммерческая (проприетарная) лицензия

Если вы хотите использовать этот код в проприетарном (закрытом) коммерческом продукте и не можете или не хотите соблюдать условия GPLv3, вам необходимо приобрести коммерческую лицензию.

Для получения информации о коммерческом лицензировании, пожалуйста, свяжитесь с автором проекта.
Подробности коммерческой лицензии описаны в файле **`LICENSE.Commercial`**.

**Авторские права:** Copyright (c) 2025 Shepard199.
